[flake8]
max-line-length = 120
exclude = .git,__pycache__,.venv,build,dist
per-file-ignores =
    # __init__.py re-exports the public API; the imports are intentionally unused there.
    polygon_api/__init__.py:F401

[tool:pytest]
testpaths = tests
